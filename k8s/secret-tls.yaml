apiVersion: v1
kind: Secret
metadata:
  name: telltale-notification-tls
  namespace: telltale-notification
  labels:
    app.kubernetes.io/name: telltale-notification
type: kubernetes.io/tls
data:
  # These are placeholder values - you MUST generate real certificates
  # See README.md for instructions on generating TLS certificates
  # After generating, encode with: cat tls.crt | base64 -w0
  tls.crt: ""
  tls.key: ""
